# Metalyzi Environment Variables

## Supabase (Required)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
# Service role key — server-side only, never expose to browser
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Auth Hook secret — from Supabase Dashboard → Authentication → Hooks → Send Email
# Set this after wiring up the suppress-email-hook endpoint to verify requests
SUPABASE_HOOK_SECRET=your_supabase_hook_secret

## Brevo Integration (for waitlist sync and transactional emails)
# Get your API key from Brevo Dashboard → Settings → API Keys
BREVO_API_KEY=your_brevo_api_key
# Must be a verified sender in your Brevo account
BREVO_SENDER_EMAIL=noreply@metalyzi.co.uk
# Reply-To address shown to recipients (use a real inbox to improve deliverability)
BREVO_REPLY_TO_EMAIL=hello@metalyzi.co.uk

## Firecrawl (property scraping - runs in parallel with Jina, free tier: 500 pages/month)
# Get your API key from firecrawl.dev
FIRECRAWL_API_KEY=your_firecrawl_api_key

## Jina Reader (property scraping - free tier available, higher limits with key)
# Get your API key from jina.ai

## EPC API (floor area lookup — free, register at https://get-energy-performance-data.communities.gov.uk)
# Bearer token from the new EPC data service (migrated from epc.opendatacommunities.org)
EPC_API_TOKEN=your_epc_bearer_token

## PropertyData API (UK property market data — replaces SpareRoom/Airbnb actors)
# Register at https://propertydata.co.uk — provides HMO rents, rental comparables,
# sold prices, asking prices, council tax, flood risk
PROPERTYDATA_API_KEY=your_propertydata_api_key

## Backend API (Flask on Render)
# This is used for AI analysis and advanced scraping
BACKEND_API_URL=https://metusa-deal-analyzer.onrender.com

## Paddle Payments
# Client-side token — from Paddle Dashboard → Developer → Authentication
# Sandbox tokens start with "test_", production with "live_"
NEXT_PUBLIC_PADDLE_CLIENT_TOKEN=test_a8e11e385f44173a28256226ebc
NEXT_PUBLIC_PADDLE_ENV=sandbox

# Price IDs — from Paddle Dashboard → Catalogue → Prices
# Create one price per plan, copy the price ID (format: pri_xxxxxxxxxxxxxxxx)
NEXT_PUBLIC_PADDLE_PRICE_PAY_PER_DEAL=pri_xxxxxxxxxxxxxxxx
NEXT_PUBLIC_PADDLE_PRICE_PRO=pri_xxxxxxxxxxxxxxxx
NEXT_PUBLIC_PADDLE_PRICE_UNLIMITED=pri_xxxxxxxxxxxxxxxx

# Server-side webhook secret — from Paddle Dashboard → Developer → Notifications
# Used to verify webhook signatures in production
PADDLE_WEBHOOK_SECRET=your_paddle_webhook_secret

## Crisp Live Chat (optional — bubble simply doesn't appear if unset)
# Get the Website ID from Crisp Dashboard → Settings → Website → Website ID
# Format: 8-4-4-4-12 UUID e.g. 12345678-1234-1234-1234-123456789012
NEXT_PUBLIC_CRISP_WEBSITE_ID=your_crisp_website_id

## AI Gateway (model sovereignty — see lib/aiGateway.ts)
# Swap the provider/model platform-wide from one place. The Metalyzi
# intelligence layer injects proprietary context through this gateway, so the
# accumulated expertise is independent of whichever model is selected.
AI_PROVIDER=anthropic
# Default model. Use claude-opus-4-8 for highest-quality analysis, or
# claude-sonnet-4-6 for lower cost. Honoured by both the TS and Python gateways.
AI_MODEL=claude-sonnet-4-6

# Bright Data Scraping Browser — Rightmove scraper (backup for Apify).
# Same variable names the backend SpareRoom scraper uses (see
# metusa-deal-analyzer render.yaml). Set EITHER the full WS endpoint:
#   BRIGHTDATA_BROWSER_WS=wss://brd-customer-<id>-zone-<zone>:<password>@brd.superproxy.io:9222
# OR the parts (assembled automatically, -country-gb appended):
BRIGHTDATA_USERNAME=brd-customer-your_id-zone-your_zone
BRIGHTDATA_PASSWORD=your_zone_password
BRIGHTDATA_HOST=brd.superproxy.io
BRIGHTDATA_PORT=9222
BRIGHTDATA_COUNTRY=gb
