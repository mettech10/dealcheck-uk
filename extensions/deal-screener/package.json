{
  "name": "metalyzi-deal-screener",
  "version": "0.1.0",
  "private": true,
  "description": "Closed-beta Metalyzi Deal Screener Chrome MV3 extension (unpacked load only).",
  "scripts": {
    "build": "node esbuild.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  }
}
